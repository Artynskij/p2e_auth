export type allDataDeal = {
    arbitration: boolean,
    client: number,
    confirmation_of_deal: boolean,
    count_of_service: number,
    id: number,
    price_for_service: string,
    result_of_deal: string,
    service: number,
    transaction_finish_date: string,
    transaction_start_date: string
}