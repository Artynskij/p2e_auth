export class postDealCredential {
    constructor(
        // price_for_service: string,
        client: number,
        service:number,
        count_of_service:number
    ) { }
}
export class putDealCredential {
    constructor(
        // price_for_service: string,
        client: number,
        service:number,
        count_of_service:number
    ) { }
}
export class patchDealCredential {
    constructor(
      
        confirmation_of_deal?:boolean,
        arbitration?:boolean

    ) { }
}
